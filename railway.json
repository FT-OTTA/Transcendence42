{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd nodejs && npm install"
  },
  "deploy": {
    "startCommand": "cd nodejs && npm start",
    "restartPolicyType": "ON_FAILURE"
  }
}